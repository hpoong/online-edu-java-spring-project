
server:
  port: 19000


logging:
  config: classpath:logback/logback-srping.xml
  level:
    root: info


spring:
  kafka:
    consumer:
      properties:
        spring.json.fail-on-empty-beans: false